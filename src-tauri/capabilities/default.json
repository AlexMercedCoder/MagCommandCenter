{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Mag Command Center.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
